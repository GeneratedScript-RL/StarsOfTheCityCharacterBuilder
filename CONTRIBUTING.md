# Contributing

Thanks for improving the builder.

## Setup

```bash
npm install
npm run dev
```

## Before Opening a Pull Request

```bash
npm run lint
npm run build
```

Keep changes focused, avoid committing generated build output, and do not commit `.env` files or API keys.

## Data Updates

Rule-facing constants live in `src/data/gameData.ts`.
